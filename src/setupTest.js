// Importing the enzyme and the Adapter.
import { configure } from "enzyme";
import Adapter from "enzyme-adapter-react-16";

// Setting up the Enzyme test environment.
configure({ adapter: new Adapter() });
